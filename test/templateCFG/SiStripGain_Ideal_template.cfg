process ICALIB = {
    
    untracked PSet maxEvents = {untracked int32 input = 1}    
    source = EmptySource {
	untracked uint32 firstRun = 1 
	untracked uint32 numberEventsInRun = 1
    }
    
    service = MessageLogger {
	untracked vstring destinations = { "Gain_Ideal.log" }
	untracked PSet Gain_Ideal.log = { untracked string threshold = "INFO" }
    }
    
    # tracker geometry    
    include "Geometry/TrackerNumberingBuilder/data/trackerNumberingGeometry.cfi"   
    include "Geometry/TrackerRecoData/data/trackerRecoGeometryXML.cfi"
    include "Geometry/TrackerGeometryBuilder/data/trackerGeometry.cfi"

    service = Timing {}
    
    service = PoolDBOutputService{
	string connect = "insert_connectstring"

	#string connect = "oracle://devdb10/CMS_COND_STRIP"
	#string connect = "sqlite_file:dbfile.db"
	#string connect = "oracle://cms_orcoff_int2r/CMS_COND_STRIP" 

	string timetype = "runnumber"    
	#string timetype = "timestamp"    
	
	untracked string BlobStreamerName="TBufferBlobStreamingService"
	PSet DBParameters = {
	    untracked string authenticationPath="/afs/cern.ch/cms/DB/conddb"
	    untracked int32 messageLevel=2
    }
	
    VPSet toPut={ { string record = "SiStripApvGainRcd"   string tag ="insert_tag_Gain_Ideal"} }    
    }
    
    #gain            
    module prod =  SiStripGainRandomCalculator {
	
	double MinPositiveGain = 0.1
	double MeanGain    = 1
	double SigmaGain   = 0
	untracked bool   printDebug = true
	
	bool SinceAppendMode = true
	string IOVMode	     = "Run"
	string Record        = "SiStripApvGainRcd"
	bool doStoreOnDB     = true
    
    }
    
    path p = { prod }
    
    module print = AsciiOutputModule {}
    endpath ep = { print }
}

